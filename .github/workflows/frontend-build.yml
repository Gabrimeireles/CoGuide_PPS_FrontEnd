name: Frontend - Build & Push

on:
  push:
    branches: [master, dev, homologation]
  pull_request:
    branches: [master, dev, homologation]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: frontend-build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    uses: Gabrimeireles/reusable-workflows/.github/workflows/docker-build.yml@v1.5
    with:
      image_name: coguide-frontend
      dockerfile: Dockerfile
      context: .
      node_version: '20'
